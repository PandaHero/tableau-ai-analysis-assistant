# Certificate Manager Requirements

# 核心依赖
certifi>=2023.0.0  # 证书包
cryptography>=41.0.0  # 证书生成和操作
PyYAML>=6.0  # 配置文件解析

# 可选依赖(用于证书验证和解析)
pyOpenSSL>=23.0.0  # 证书验证和解析

# 测试依赖
hypothesis>=6.0.0  # 基于属性的测试
pytest>=7.0.0  # 测试框架

# 如果项目已经安装了这些库,无需重复安装
# requests>=2.28.0
# httpx>=0.24.0
# aiohttp>=3.8.0
