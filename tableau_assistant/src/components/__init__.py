"""
Components Layer

Components are code modules that are not exposed to LLM as tools.
They are called directly by code (nodes or other components).

Components may internally use LLM, but expose code interfaces externally.
"""
