# -*- coding: utf-8 -*-
"""
无关问题检测正则模式

用于 IntentRouter 检测与数据分析无关的问题。

规则模式是领域知识，类似种子数据，不应放入 app.yaml 配置文件。
配置文件只存放阈值、超时等运行时参数。

用法：
    from analytics_assistant.src.infra.seeds import IRRELEVANT_PATTERNS
"""

# ══════════════════════════════════════════════════════════════
# IntentRouter 无关问题检测模式
# ══════════════════════════════════════════════════════════════

# 明确无关的模式（高置信度拒绝）- 正则表达式
# 匹配这些模式时，返回 IRRELEVANT 意图
IRRELEVANT_PATTERNS: list[str] = [
    # ─────────────────────────────────────────────────────────
    # 简单问候/告别（完整匹配）
    # ─────────────────────────────────────────────────────────
    r"^(你好|您好|hi|hello|hey|嗨|哈喽)[\s,，.。!！?？]*$",
    r"^(谢谢|感谢|thanks|thank you)[\s,，.。!！?？]*$",
    r"^(再见|拜拜|bye|goodbye)[\s,，.。!！?？]*$",
    r"^(好的|ok|okay|嗯|哦|明白|知道了|收到)[\s,，.。!！?？]*$",
    
    # ─────────────────────────────────────────────────────────
    # 闲聊/生活类问题
    # ─────────────────────────────────────────────────────────
    r"(天气|天气预报|气温)",
    r"(讲个笑话|说个笑话|来个笑话)",
    r"(你是谁|你叫什么|介绍.*你自己)",
    
    # ─────────────────────────────────────────────────────────
    # 创作类请求
    # ─────────────────────────────────────────────────────────
    r"(帮我写|写一篇|写一个).*(文章|故事|诗|小说)",
    r"(翻译|translate)",
    
    # ─────────────────────────────────────────────────────────
    # 新闻/娱乐类
    # ─────────────────────────────────────────────────────────
    r"(新闻|热点|头条|八卦)",
    r"(电影|音乐|游戏|小说)推荐",
    r"推荐.*(电影|音乐|游戏|小说|书)",
    
    # ─────────────────────────────────────────────────────────
    # 金融/投资类（非数据分析）
    # ─────────────────────────────────────────────────────────
    r"(股票|基金|理财|投资建议|炒股)",
    
    # ─────────────────────────────────────────────────────────
    # 生活服务类
    # ─────────────────────────────────────────────────────────
    r"(菜谱|食谱|做菜|烹饪)",
]

__all__ = [
    "IRRELEVANT_PATTERNS",
]
